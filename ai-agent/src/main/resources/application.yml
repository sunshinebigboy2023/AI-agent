spring:
  application:
    name: office-ai-assistant
  profiles:
    active: local
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY:}
      chat:
        options:
          model: qwen-plus
    ollama:
      base-url: http://localhost:11434
      chat:
        model: gemma3:1b
# 临时注释掉，便于大家开发调试和部署
#    mcp:
#      client:
#        sse:
#          connections:
#            server1:
#              url: http://localhost:8127
#        stdio:
#          servers-configuration: classpath:mcp-servers.json
#    vectorstore:
#      pgvector:
#        index-type: HNSW
#        dimensions: 1536
#        distance-type: COSINE_DISTANCE
#        max-document-batch-size: 10000 # Optional: Maximum number of documents per batch
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi config
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.yupi.yuaiagent.controller
# knife4j config
knife4j:
  enable: true
  setting:
    language: zh_cn
aliyun:
  opensearch:
    host: ${ALIYUN_OPENSEARCH_HOST:}
    workspace: ${ALIYUN_OPENSEARCH_WORKSPACE:default}
    service-id: ${ALIYUN_OPENSEARCH_SERVICE_ID:ops-web-search-001}
    api-key: ${ALIYUN_OPENSEARCH_API_KEY:}
    top-k: ${ALIYUN_OPENSEARCH_TOP_K:5}
    content-type: ${ALIYUN_OPENSEARCH_CONTENT_TYPE:snippet}
searxng:
  base-url: ${SEARXNG_BASE_URL:}
  top-k: ${SEARXNG_TOP_K:5}
office:
  web:
    allowed-origin-patterns: http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173
  agent:
    enable-terminal-tool: false
  knowledge:
    storage-dir: ${OFFICE_KNOWLEDGE_DIR:${user.dir}/tmp/knowledge}
    max-upload-bytes: ${OFFICE_KNOWLEDGE_MAX_UPLOAD_BYTES:10485760}
# 修改日志级别，查看 Spring AI 更多调用细节
logging:
  level:
    org.springframework.ai: DEBUG
